def run_trial(diff: str, timeout_s: int) -> dict:
    """Stub trial runner: apply diff, execute bounded run, return metrics."""
    return {"status": "stub", "timeout_s": timeout_s, "diff_len": len(diff)}
